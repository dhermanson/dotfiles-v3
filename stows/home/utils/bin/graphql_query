#! /usr/bin/env bash

url=$1
file=$2

curl \
  -v \
  -X POST $url  \
  --data "{\"query\": \"$(cat $file | tr -d '[:cntrl:]' | sed -E 's/"/\\"/g') \"}"  \
  -H "Content-Type: application/json"
  # -H "Accept: application/json" \
  # | python3 -m json.tool
